export { TextInput } from './TextInput';
export type { TextInputProps, TextInputRef } from './types';
export { UNSUPPORTED_PROPS, type UnsupportedPropName } from './unsupported';

// Exported so the style mapping can be unit-tested and reasoned about
// independently of the components.
export { splitStyle, type SplitStyle } from './splitStyle';
